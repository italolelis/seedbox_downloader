---
phase: 14-transfer-cleanup-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/seedbox_downloader/main.go
  - internal/downloader/downloader.go
  - internal/dc/putio/client.go
autonomous: true

requirements:
  - CLN-01
  - CLN-02

must_haves:
  truths:
    - "After Sonarr/Radarr confirms import, the Put.io transfer is deleted (not left indefinitely)"
    - "The Put.io file data associated with the transfer is deleted alongside the transfer"
    - "When PUTIO_SEED_RATIO is unset or zero, cleanup fires immediately after import confirmation"
    - "When PUTIO_SEED_RATIO is set, cleanup fires after the live upload ratio reaches or exceeds the configured value"
    - "If the Put.io cleanup API call returns 'transfer not found', it is treated as success (desired state achieved)"
    - "If cleanup retries are exhausted, error is logged and pipeline continues — no crash or stall"
    - "Transfers that fail import are not affected by the cleanup logic"
  artifacts:
    - path: "cmd/seedbox_downloader/main.go"
      provides: "PUTIO_SEED_RATIO config field; cleanupTransfer helper with backoff; handleTransferImported routing"
      contains: "PUTIO_SEED_RATIO"
    - path: "internal/downloader/downloader.go"
      provides: "Fixed WatchForSeeding that polls live ratio and triggers cleanup at threshold"
      contains: "seedRatio"
    - path: "internal/dc/putio/client.go"
      provides: "GetTransferInfo method returning live upload_ratio for a given transfer ID"
      contains: "GetTransferInfo"
  key_links:
    - from: "cmd/seedbox_downloader/main.go:handleTransferImported"
      to: "internal/downloader/downloader.go:WatchForSeeding"
      via: "called when PUTIO_SEED_RATIO > 0"
      pattern: "WatchForSeeding"
    - from: "internal/downloader/downloader.go:WatchForSeeding"
      to: "internal/dc/putio/client.go:GetTransferInfo"
      via: "type assertion d.dc.(*putio.Client)"
      pattern: "GetTransferInfo"
    - from: "cmd/seedbox_downloader/main.go:cleanupTransfer"
      to: "transfer.TransferClient:RemoveTransfers"
      via: "backoff.Retry with deleteFiles=true"
      pattern: "RemoveTransfers.*true"
---

<objective>
Fix Put.io transfer and file data cleanup after Sonarr/Radarr confirms import.

Purpose: Currently transfers are never cleaned up from Put.io because WatchForSeeding polls a stale local snapshot that never changes status. This fix implements two paths: (1) immediate cleanup when no seed ratio is configured, and (2) ratio-based cleanup when PUTIO_SEED_RATIO is set — both using exponential backoff and treating "transfer not found" as success.
Output: Put.io transfers and their file data are deleted after import confirmation. PUTIO_SEED_RATIO env var controls optional seeding window before cleanup.
</objective>

<execution_context>
@/Users/italovietro/.claude/get-shit-done/workflows/execute-plan.md
@/Users/italovietro/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-transfer-cleanup-fix/14-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PUTIO_SEED_RATIO config and immediate cleanup path in main.go</name>
  <files>cmd/seedbox_downloader/main.go</files>
  <action>
    1. Add `PutioSeedRatio float64` field to the `config` struct with envconfig tag `PUTIO_SEED_RATIO` and default `0` (e.g. `PutioSeedRatio float64 \`envconfig:"PUTIO_SEED_RATIO" default:"0"\``).

    2. Add a `cleanupTransfer` helper function (package-level, not a method) with signature:
       ```go
       func cleanupTransfer(ctx context.Context, logger *slog.Logger, tc transfer.TransferClient, t *transfer.Transfer)
       ```
       Inside:
       - Compute the SHA1 hash of `t.ID` (same pattern as existing `WatchForSeeding`): `hash := sha1.Sum([]byte(t.ID)); hashStr := hex.EncodeToString(hash[:])`
       - Use `backoff.Retry` from `github.com/cenkalti/backoff/v5` with `backoff.WithMaxTries(backoff.NewExponentialBackOff(), 3)` to call `tc.RemoveTransfers(ctx, []string{hashStr}, true)`.
       - In the retry operation: if the error message contains `"transfer not found"` (string match), log at info level ("transfer already removed from Put.io, treating as success") and return `nil` to stop retrying — the desired state is achieved.
       - If `backoff.Retry` exhausts all attempts: log at error level ("cleanup failed after retries, continuing pipeline") with `transfer_id`, `transfer_name`, and the error. Do NOT return an error — pipeline must continue.
       - On success: log at info level ("Put.io transfer and files cleaned up") with `transfer_id` and `transfer_name`.

    3. Modify `handleTransferImported` to accept `seedRatio float64` as a new parameter and route accordingly:
       - **When `seedRatio == 0`**: call `cleanupTransfer(ctx, logger, dl.tc, t)` directly (use `dl.tc` — the `TransferClient` field on `Downloader`, which is already accessible as a field). Note: `Downloader.tc` is currently unexported. Add a `TC() transfer.TransferClient` accessor method on `*downloader.Downloader` if needed, OR pass `tc transfer.TransferClient` as a parameter to `handleTransferImported`. Prefer passing `tc` as a parameter to keep `handleTransferImported` a pure function.
       - **When `seedRatio > 0`**: call `dl.WatchForSeeding(ctx, t, pollingInterval, seedRatio)` (updated signature — see Task 2).
       - Remove the existing `dl.WatchForSeeding` call that was unconditional.

    4. Update the call site in `setupNotificationForDownloader` where `handleTransferImported` is invoked — pass `cfg.PutioSeedRatio` and the instrumented transfer client `instrumentedTC`. To make `instrumentedTC` available in `setupNotificationForDownloader`, add it as a parameter to that function and thread it from `initializeServices`.

    5. Log `putio_seed_ratio` in the configuration loaded log statement at startup (in the existing `logger.InfoContext(ctx, "configuration loaded", ...)` block).

    Note: `cenkalti/backoff/v5` is already in go.mod. Import it as `"github.com/cenkalti/backoff/v5"`. Check the v5 API — use `backoff.WithMaxTries(backoff.NewExponentialBackOff(), 3)` and `backoff.Retry(operation, b)`. If the v5 API differs, use the actual v5 signatures (e.g. `backoff.RetryWithData` or `backoff.Retry` accepting a context — check the library and use whichever compiles).
  </action>
  <verify>
    `cd /Users/italovietro/projects/seedbox_downloader && go build ./...` — must compile with no errors.
    `go vet ./...` — must produce no warnings.
  </verify>
  <done>
    - `config` struct has `PutioSeedRatio float64` field with envconfig tag `PUTIO_SEED_RATIO`
    - `cleanupTransfer` function exists with backoff retry (max 3 attempts), "not found" = success, exhausted = log error + continue
    - `handleTransferImported` routes to immediate cleanup when ratio is 0, or `WatchForSeeding` when ratio > 0
    - `go build ./...` passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix WatchForSeeding to poll live Put.io ratio and add GetTransferInfo to putio.Client</name>
  <files>
    internal/downloader/downloader.go
    internal/dc/putio/client.go
  </files>
  <action>
    **internal/dc/putio/client.go — add GetTransferInfo:**

    Add a new exported method `GetTransferInfo` on `*Client`:
    ```go
    // GetTransferInfo returns the current upload ratio for a Put.io transfer by its internal numeric ID string.
    // Returns (0, nil) if the transfer no longer exists on Put.io (treated as cleanup-complete by caller).
    func (c *Client) GetTransferInfo(ctx context.Context, transferID string) (uploadRatio float64, found bool, err error)
    ```
    - Call `c.putioClient.Transfers.List(ctx)` to get all transfers.
    - Find the transfer where `fmt.Sprintf("%d", t.ID) == transferID`.
    - If not found: return `0, false, nil`.
    - If found: return `float64(t.UploadRatio)` (check the actual field name on `putio.Transfer` — it may be `UploadRatio`, `Ratio`, or similar; look at the go-putio struct fields), `true, nil`.
    - If `Transfers.List` errors: return `0, false, err`.

    Note: The `putio.Transfer` struct fields can be checked by grepping the go-putio vendor/module. The upload ratio field on Put.io transfers is `UploadRatio` (float32 or float64 — cast appropriately).

    **internal/downloader/downloader.go — fix WatchForSeeding:**

    Update `WatchForSeeding` signature to accept `seedRatio float64`:
    ```go
    func (d *Downloader) WatchForSeeding(ctx context.Context, t *transfer.Transfer, pollingInterval time.Duration, seedRatio float64)
    ```

    Replace the body logic entirely:
    - Keep the goroutine, panic recovery, ticker, and context.Done() patterns (unchanged).
    - In the ticker case: call `dc.GetTransferInfo(ctx, t.ID)` via the existing type assertion `dc, ok := d.dc.(*putio.Client)`. If `!ok`, log error and return (same as before).
    - If `GetTransferInfo` returns `found=false`: log info ("transfer no longer exists on Put.io, cleanup already done"), return from goroutine.
    - If `GetTransferInfo` errors: log error and `continue` (retry next tick).
    - If `uploadRatio >= seedRatio`: log info ("seed ratio reached, cleaning up transfer", "upload_ratio", uploadRatio, "target_ratio", seedRatio), then call `cleanupTransfer` — but `cleanupTransfer` is defined in `main.go`. Instead, inline the cleanup call here: use the same SHA1 hash pattern and call `d.tc.RemoveTransfers(ctx, []string{hashStr}, true)` directly with a simple retry loop (up to 3 attempts, 2s sleep between) since backoff is not imported in the downloader package. Log errors, treat "transfer not found" as success, return after cleanup.

    Actually, to avoid duplicating retry logic: define a `cleanupTransferWithRetry` function in `internal/downloader/downloader.go` (unexported) that takes `(ctx context.Context, tc transfer.TransferClient, transferID string, logger *slog.Logger)` and implements the backoff retry + "not found" = success logic using `cenkalti/backoff/v5`. This avoids the logic being split across two packages. Then in `main.go`'s `cleanupTransfer`, call `cleanupTransferWithRetry` — actually no, that would create an import cycle.

    Best approach: Put the retry helper in `internal/downloader/downloader.go` as an unexported function `cleanupWithRetry(ctx, tc, transferID string, logger)`. In `main.go`, `cleanupTransfer` calls `d.CleanupTransfer(ctx, t)` — expose `CleanupTransfer` as a method on `*Downloader`. This keeps retry logic in one place.

    Revised approach:
    - In `internal/downloader/downloader.go`: Add `CleanupTransfer(ctx context.Context, t *transfer.Transfer)` method on `*Downloader`. This method computes the SHA1 hash, calls `d.tc.RemoveTransfers(ctx, []string{hashStr}, true)` with backoff retry (cenkalti/backoff/v5, max 3 attempts). Treats "transfer not found" error as success (info log). On exhausted retries: log error, return (do not panic).
    - Update `WatchForSeeding` to call `d.CleanupTransfer(ctx, t)` when ratio is reached.
    - In `main.go` `cleanupTransfer` helper: call `dl.CleanupTransfer(ctx, t)` where `dl *downloader.Downloader` is passed as a parameter (thread through from `handleTransferImported` which already receives `dl *downloader.Downloader`).
    - Update `handleTransferImported` in main.go to call `dl.CleanupTransfer(ctx, t)` directly (immediate path) or `dl.WatchForSeeding(ctx, t, pollingInterval, seedRatio)` (ratio path). Remove the standalone `cleanupTransfer` helper from main.go since logic lives on Downloader now.

    This is the clean design. Implement it:

    **internal/downloader/downloader.go changes:**
    1. Add `import "github.com/cenkalti/backoff/v5"` and `"strings"` to imports.
    2. Add `CleanupTransfer(ctx context.Context, t *transfer.Transfer)` method:
       - Compute `hash := sha1.Sum([]byte(t.ID)); hashStr := hex.EncodeToString(hash[:])`
       - Use backoff retry (max 3 attempts) calling `d.tc.RemoveTransfers(ctx, []string{hashStr}, true)`
       - If error contains "transfer not found": log info ("Put.io transfer already removed, treating as success", "transfer_id", t.ID, "transfer_name", t.Name), return nil from operation func
       - On exhausted retries: log error ("failed to clean up Put.io transfer after retries, continuing", "transfer_id", t.ID, "err", err) and return (don't crash)
       - On success: log info ("Put.io transfer and files cleaned up", "transfer_id", t.ID, "transfer_name", t.Name)
    3. Update `WatchForSeeding(ctx, t, pollingInterval, seedRatio float64)`:
       - Replace stale `t.IsSeeding()` check with live ratio polling via type assertion to `*putio.Client`
       - Call `putioClient.GetTransferInfo(ctx, t.ID)` in each tick
       - If not found: log info and return
       - If error: log error, continue
       - If `uploadRatio >= seedRatio`: call `d.CleanupTransfer(ctx, t)`, return
    4. Add `"github.com/italolelis/seedbox_downloader/internal/dc/putio"` import (already present via existing type assertion).

    **cmd/seedbox_downloader/main.go changes:**
    1. Add `PutioSeedRatio float64` to config with envconfig:"PUTIO_SEED_RATIO" default:"0".
    2. Add `putio_seed_ratio` to the startup configuration log.
    3. Simplify `handleTransferImported`: when `seedRatio == 0` call `dl.CleanupTransfer(ctx, t)`; when `seedRatio > 0` call `dl.WatchForSeeding(ctx, t, pollingInterval, seedRatio)`. Update signature to accept `seedRatio float64`.
    4. Update `setupNotificationForDownloader` call site (and function signature) to pass `cfg.PutioSeedRatio`.
    5. Remove any redundant SHA1/hex imports that were only used in `WatchForSeeding` (if they existed in main.go — verify before removing).
  </action>
  <verify>
    `cd /Users/italovietro/projects/seedbox_downloader && go build ./...` — no errors.
    `go vet ./...` — no warnings.
    `go test ./internal/downloader/... ./internal/dc/putio/... ./cmd/...` — existing tests pass (no new tests required in this plan; the behavior is integration-level and covered by the running system).
    Verify `WatchForSeeding` no longer references `t.IsSeeding()`.
    Verify `CleanupTransfer` method exists on `*Downloader`.
    Verify `GetTransferInfo` method exists on `*putio.Client`.
  </verify>
  <done>
    - `*putio.Client` has `GetTransferInfo(ctx, transferID string) (float64, bool, error)` that polls live ratio
    - `*Downloader` has `CleanupTransfer(ctx, t)` with backoff retry and "not found" = success semantics
    - `WatchForSeeding` signature is `(ctx, t, pollingInterval, seedRatio float64)` and uses live polling
    - `handleTransferImported` routes: `seedRatio == 0` → immediate `CleanupTransfer`, `seedRatio > 0` → `WatchForSeeding`
    - `PUTIO_SEED_RATIO` env var present in config struct
    - `go build ./...` and `go test ./...` both pass
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/italovietro/projects/seedbox_downloader && go build ./...` must pass with no errors
2. `go vet ./...` must produce no warnings
3. `go test ./...` must pass (all existing tests green)
4. Grep check: `grep -n "IsSeeding" internal/downloader/downloader.go` must return no results (stale polling removed)
5. Grep check: `grep -n "CleanupTransfer" internal/downloader/downloader.go` must show the method definition
6. Grep check: `grep -n "GetTransferInfo" internal/dc/putio/client.go` must show the method definition
7. Grep check: `grep -n "PUTIO_SEED_RATIO" cmd/seedbox_downloader/main.go` must show the config field
8. Grep check: `grep -n "transfer not found" internal/downloader/downloader.go` must show the "not found" handling
</verification>

<success_criteria>
- CLN-01: After Sonarr/Radarr import confirmation, Put.io transfer is deleted (not left indefinitely). With PUTIO_SEED_RATIO=0 (default), cleanup fires immediately; with PUTIO_SEED_RATIO set, cleanup fires after ratio is reached.
- CLN-02: Put.io file data is deleted alongside transfer removal — `RemoveTransfers` is called with `deleteFiles=true` in all cleanup paths.
- Transfers that fail import are not affected — cleanup is only triggered from the `OnTransferImported` channel, which is only written after successful import detection.
- Cleanup failures (API errors) do not crash the pipeline — they are logged and the pipeline continues.
- Transfer-not-found is treated as success — no spurious error logs for transfers already cleaned manually.
</success_criteria>

<output>
After completion, create `.planning/phases/14-transfer-cleanup-fix/14-01-SUMMARY.md` following the summary template.
</output>
